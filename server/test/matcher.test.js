const Matcher = require("../app/matcher");

describe("Matcher", () => {
    let matcher;
    
    beforeEach(() => {
        matcher = new Matcher();
    });

    it("fails this test", () => {
        expect(true).toBe(false);
    });
});